source "https://rubygems.org"

# fastlane drives the build → TestFlight / App Store upload, including
# the "What's New" release notes. Pinned to a recent minor so CI runs
# are reproducible; bump deliberately.
gem "fastlane", "~> 2.222"
